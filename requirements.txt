# Pinned to the versions verified running in production (railway pip freeze, 2026-07-06).
# Bump deliberately, one at a time — never let a Railway rebuild pick versions for you.
Flask==3.1.3
Flask-WTF==1.3.0
Flask-Limiter==4.1.1
anthropic==0.116.0
requests==2.34.2
bleach==6.4.0
pillow==12.3.0
posthog==7.21.3
tzdata==2026.2
